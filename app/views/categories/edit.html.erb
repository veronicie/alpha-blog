<h1 class="mt-4 mb-4 text-center">Edit category name</h1>

<div class="container">
  <%= render 'form' %>
  <p class="mt-4"><%= link_to '[ Cancel and return to category ]', category_path(@category), class: "text-warning" %></p>
</div>